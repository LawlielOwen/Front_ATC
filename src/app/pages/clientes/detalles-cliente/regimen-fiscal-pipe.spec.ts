import { RegimenFiscalPipe } from './regimen-fiscal-pipe';

describe('RegimenFiscalPipe', () => {
  it('create an instance', () => {
    const pipe = new RegimenFiscalPipe();
    expect(pipe).toBeTruthy();
  });
});
